/**
 * Shared module - cross-cutting code (models, constants)
 *
 * Contains shared types, interfaces, and constants used across the application.
 * This module can only depend on util/.
 *
 * @module shared
 * @see mddocs/frontend/frontend-tdd.md#folder-layout
 */

// Placeholder - shared exports will be added as shared code is implemented
export type SharedLibrary = 'models' | 'constants';

/**
 * Features module - routable feature components
 *
 * Feature modules contain route components that compose UI and data-access layers.
 * Each feature is a self-contained vertical slice of the application.
 *
 * @module features
 * @see mddocs/frontend/frontend-tdd.md#folder-layout
 */

// Placeholder - feature exports will be added as features are implemented
export type FeatureModule = 'session-list' | 'session' | 'join-session';

/**
 * Markdown pipe module.
 *
 * Provides the MarkdownPipe for rendering markdown content as HTML.
 * This is extracted into its own module to avoid circular dependencies
 * between data-tree and smart-blob components.
 *
 * @module ui/shared/markdown-pipe
 */

export { MarkdownPipe } from './markdown.pipe';
